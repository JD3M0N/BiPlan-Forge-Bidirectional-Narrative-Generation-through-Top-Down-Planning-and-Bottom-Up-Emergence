[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "asg-core"
version = "0.1.0"
description = "Shared infrastructure for the ASG monorepo"
readme = "README.md"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/asg_core"]

